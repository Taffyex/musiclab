[project]
name = "musiclab"
version = "0.1.0"
description = "AI-powered music discovery and library management"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
